# Página · Movimientos

Base: `../MASTER.md`. Ajustes:

- Hero con `.brand-gradient` (mes/año + "Total gastado" en blanco).
- Lista agrupada por día; ingreso `text-positive`, gasto `text-negative`,
  transferencia `text-muted-foreground`.
- Filtros en `DrawerSelector`/Popover; sin tablas anchas.
- FAB `+` (gradiente) abre captura rápida (Tipo · Cantidad · Descripción).
